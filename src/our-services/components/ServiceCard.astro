---
interface Props {
    icon: string;
    title: string;
    text: string;
}

const { icon, title, text } = Astro.props;
---

<article class="card">
    <img src={icon} alt={title} />
    <h4>{title}</h4>
    <p>{text}</p>
</article>
